import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  root: {
    flexDirection: 'row',
    marginBottom: 24,
    marginHorizontal: 32,
    alignItems: 'center',
  },
  icon: {
    marginRight: 16,
  },
});

export default styles;
